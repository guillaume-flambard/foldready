# audit-fidelity

Raise audit accuracy: lex Swift files instead of matching lines, promote the two missing Apple audit areas to first-class checks, and give every finding a confidence level so the gate can act on the ones it can defend.
